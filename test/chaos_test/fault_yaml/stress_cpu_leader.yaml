apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  creationTimestamp: null
  generateName: stress-chaos-
  namespace: default
spec:
  duration: ${duration}
  mode: all
  selector:
    namespaces:
    - ${NAMESPACE}
    pods:
      ${NAMESPACE}:
      - ${leader_pod}
  stressors:
    cpu:
      load: 100
      workers: 5
    memory:
      workers: 0
status:
  experiment: {}
