apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  creationTimestamp: null
  generateName: pod-chaos-
  namespace: default
spec:
  action: pod-kill
  duration: 10s
  mode: all
  selector:
    namespaces:
    - ${NAMESPACE}
    pods:
      ${NAMESPACE}:
      - ${leader_pod}
      - ${follower_pod1}
status:
  experiment: {}
