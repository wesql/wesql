apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  creationTimestamp: null
  generateName: network-chaos-
  namespace: default
spec:
  action: duplicate
  direction: to
  duplicate:
    correlation: "100"
    duplicate: "100"
  duration: ${duration}
  mode: all
  selector:
    namespaces:
    - ${NAMESPACE}
    pods:
      ${NAMESPACE}:
      - ${pod_name1}
      - ${pod_name2}
      - ${pod_name3}
status:
  experiment: {}
